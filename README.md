
  # Coffee Shop App (Copy)

  This is a code bundle for Coffee Shop App (Copy). The original project is available at https://www.figma.com/design/qsebUUbfJWhXQcSqYeLbRj/Coffee-Shop-App--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  