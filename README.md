
  # Coffee Shop App (Iphone)

  This is a code bundle for Coffee Shop App (Iphone). The original project is available at https://www.figma.com/design/qsebUUbfJWhXQcSqYeLbRj/Coffee-Shop-App--Iphone-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  